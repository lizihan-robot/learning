git :
	分支操作：
		查看分支所有分支	git branch -a
		增加分支		git branch [branchName] 
		删除本地分支		git branch -d [branchName]
		切换分支		git checkout [branchName]
		删除远程分支		git push origin --delete [branchname]
	查看状态：
		git status
	添加修改：
		git add .
	提交修改
		git commit -m "descript"
	上传		
		git push origin <分支名>
	拉取请求
		git pull
